{
  "name": "@dsh-app/plugin-brand",
  "description": "DSH APP brand host plugin — SCAFFOLD: the brand settings namespace, app/kernel info service and desktop bridge are declared but not wired yet (see the TODOs in src/index.ts). The shell injects its desktop chrome directly, and plugin-client-ui talks to upstream remotes rather than through this plugin.",
  "version": "0.2.0",
  "type": "module",
  "main": "lib/index.js",
  "types": "lib/index.d.ts",
  "exports": {
    ".": {
      "types": "./lib/index.d.ts",
      "default": "./lib/index.js"
    },
    "./package.json": "./package.json"
  },
  "dsh": {
    "bundle": {
      "patch": "./cordis.patch.yml"
    }
  },
  "files": [
    "lib",
    "cordis.patch.yml"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json"
  },
  "peerDependencies": {
    "@deepseek-ai/cordis": "^4.0.0",
    "@deepseek-ai/dsh-settings": "^0.1.5-rc.1"
  },
  "devDependencies": {
    "@deepseek-ai/cordis": "4.0.2",
    "@deepseek-ai/dsh-settings": "0.1.5-rc.1",
    "typescript": "^5.7.2"
  },
  "license": "MIT"
}
